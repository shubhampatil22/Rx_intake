# -------------------------------------------------------
# QA environment secrets — copy to conf/qa.env and fill in real values
# conf/qa.env is gitignored; never commit real secrets
# -------------------------------------------------------

# Confluent Schema Registry
SR_API_KEY=<REPLACE_ME>
SR_SECRET=<REPLACE_ME>

# Confluent Kafka cluster
KAFKA_SASL_USERNAME=<REPLACE_ME>
KAFKA_SASL_PASSWORD=<REPLACE_ME>
