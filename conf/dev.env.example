# -------------------------------------------------------
# DEV environment secrets — copy to conf/dev.env and fill in real values
# conf/dev.env is gitignored; never commit real secrets
# -------------------------------------------------------

# Confluent Schema Registry (lsrc-7y5nr22)
SR_API_KEY=<REPLACE_ME>
SR_SECRET=<REPLACE_ME>

# Confluent Kafka cluster (lkc-12njzpv)
KAFKA_SASL_USERNAME=<REPLACE_ME>
KAFKA_SASL_PASSWORD=<REPLACE_ME>
